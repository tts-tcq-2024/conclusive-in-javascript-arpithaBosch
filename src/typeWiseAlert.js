// src/typeWiseAlert.js

const { classifyTemperatureBreach } = require('./temperatureClassification');

// Export only the functions that are used for testing purposes
module.exports = {
    classifyTemperatureBreach,
};
